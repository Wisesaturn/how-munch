# GitHub 이슈 작성 가이드

필요한 섹션만 골라 읽는다.

- **이슈 제목 규칙** — 유형 구분 기준 + 종결 어미. 유형 판단·제목 작성 시.
- **유형별 템플릿** — 기능 구현 / 상태 개선 / 버그 리포트 / 리팩토링 / 유지보수. 발행할 이슈의 **해당 유형 1종만** 펼쳐 사용.
- **gh CLI 명령어 패턴** — 이슈 생성·조회·sub-issue 연결·브랜치 반영.

---

## 이슈 제목 규칙

이슈 제목은 **한국어 문장형**으로 작성한다.

| 유형                    | 종결 형태                                          | 구분 기준                              |
| ----------------------- | -------------------------------------------------- | -------------------------------------- |
| 기능 구현 (`✨Feature`) | `~를 구현한다` / `~를 추가한다` / `~를 정의한다`  | 존재하지 않던 기능 신규 추가           |
| 상태 개선 (`🔨Fix`)     | `~를 조정한다` / `~이어야 한다` / `~처리한다`     | 사용자가 체감하는 동작·결과가 달라짐   |
| 버그 (`🐞Bug`)          | `~현상이 발생한다` / `~문제가 있다`               | 의도치 않은 오동작                     |
| 리팩토링 (`♻️Refactor`) | `~를 리팩토링한다` / `~구조를 개선한다`           | 외부 동작 동일, 내부 코드·구조만 변경  |
| 유지보수 (`🔧Chore`)    | `~를 업데이트한다` / `~를 추가한다`               | 빌드, 패키지, 문서 등                  |

### 작성 원칙

- **마침표(`.`) 없이** 종결
- 영문 고유명사는 **원문 그대로** 사용 (예: `Supabase`, `RPC`, `TanStack Query`)
- **30자 내외**로 간결하게

---

## 유형별 템플릿

### ✨ 기능 구현 템플릿

```markdown
## 💡 기능 구현 사항

원하는 기능이나 해결 방안에 대해 구체적으로 설명합니다.

## 🗂️ 구현 위치

어떤 화면/도메인에서 구현할지 설명합니다.

## 📎 추가 정보

관련 참고 자료, 디자인 링크, 연관 이슈 등이 있다면 첨부해주세요.
```

---

### 🔨 상태 개선 템플릿

```markdown
## AS-IS

- 현재 상황을 설명합니다
- 발생한 문제점은 무엇인지 작성합니다

## TO-BE

- 수정 후 예상되는 결과를 작성합니다
- 어떻게 수정할 것인지 방향을 설명합니다

## 📎 추가 정보

관련 참고 자료나 연관 이슈가 있다면 첨부해주세요.
```

---

### 🐞 버그 리포트 템플릿

```markdown
## ✅ 체크리스트

- [ ] 비슷한 버그 리포트가 있는지 검색해보았습니다
- [ ] 문제 해결에 도움될 만한 모든 정보를 포함했습니다

### 🐞 버그 설명

발생한 버그에 대해 명확하고 간단히 설명합니다.

## 📝 재현 방법

1. ... 화면으로 이동
2. ... 버튼 클릭
3. ... 항목 선택
4. 에러 발생

## ✨ 예상했던 동작

원래 정상적으로 동작했어야 하는 방식을 설명합니다.

## 🖼️ 스크린샷 (선택)

가능하다면 문제 상황을 보여주는 스크린샷을 첨부해주세요.

## 📋 에러 메시지 (선택)

에러 메시지가 있다면 전체 내용을 복사해서 붙여넣어주세요.

## 💻 사용 환경 (선택)

- 사용 환경: 데스크톱 / 모바일
- 운영체제: (예: macOS Sonoma, iOS 18)
- 브라우저: (예: Chrome 122, Safari)

## 📎 추가 정보 (선택)

버그 발생 시각, 네트워크 환경, 관련 이슈 등
```

---

### ♻️ 리팩토링 템플릿

```markdown
## 🎯 리팩토링 목적

왜 이 코드를 리팩토링해야 하는지 설명합니다.

## 📋 대상 범위

- [ ] 함수/훅 개선
- [ ] 파일/폴더 구조 정리
- [ ] 변수/함수명 개선
- [ ] 중복 코드 제거
- [ ] 성능 최적화
- [ ] 타입 개선

## 🔄 현재 상태

현재 코드나 구조를 간단히 설명합니다.

## 🔄 개선 후

리팩토링 후 예상되는 코드나 구조를 설명합니다.

## 🧪 검증 방법

기존 기능이 정상 동작하는지 확인할 방법을 작성합니다.

## 📎 추가 정보

관련 이슈 링크, 참고 문서 등
```

---

### 🔧 유지보수 템플릿

```markdown
## 🔧 작업 내용

어떤 작업을 할 것인지 설명합니다.

- 작업 항목 A
- 작업 항목 B

## 📎 추가 정보

관련 참고 자료나 연관 이슈가 있다면 첨부해주세요.
```

---

## gh CLI 명령어 패턴

### 이슈 생성

```bash
gh issue create \
  --title "이슈 제목" \
  --body "$(cat <<'EOF'
본문 내용
EOF
)"
```

### 이슈 목록 확인

```bash
gh issue list
```

### sub-issue 연결 (네이티브 REST API)

부모 이슈와 자식 이슈를 모두 생성한 뒤, 자식의 **REST id**(이슈 번호 아님)로 연결합니다.

```bash
PARENT={부모 이슈번호}
CHILD_ID=$(gh api repos/wisesaturn/how-munch/issues/{자식 이슈번호} --jq .id)
gh api --method POST \
  repos/wisesaturn/how-munch/issues/$PARENT/sub_issues \
  -F sub_issue_id=$CHILD_ID
```

### 이슈 번호를 브랜치명에 반영

이슈 생성 후 출력된 URL에서 번호를 확인:

```
https://github.com/{owner}/how-munch/issues/{번호}
```

브랜치 네이밍:

```
feat/#{번호}
fix/#{번호}
```
